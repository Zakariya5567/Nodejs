/*
    -> npm stand for node package manager
    -> package store all the records of the application 
    -> For installing npm you  run  npm install
    -> fot initializing npm  run npm init
    -> install dependencies / packge 
    -> e.g colors is a dependency  
*/
// Don't push node_module folder on github
// if  you delete your node_module folder you can recover by  run npm install


const color=require('colors');

console.log("color dependency".red);
